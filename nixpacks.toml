[phases.setup]
nixPkgs = ["tesseract", "poppler_utils"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn contractvault_api.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
